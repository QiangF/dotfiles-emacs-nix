;;; pg-config.el --- -*- lexical-binding: t; -*-
(defconst pg/config-dir (expand-file-name "~/.emacs.profiles.d/purplg/"))
(defconst pg/module-dir (expand-file-name "modules/" pg/config-dir))
(provide 'pg-config)
